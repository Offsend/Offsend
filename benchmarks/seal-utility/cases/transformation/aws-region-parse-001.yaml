id: aws-region-parse-001
category: transformation
task: |
  Parse the AWS profile.
  Return JSON with region, output, access_key_set (boolean).
  JSON only. No markdown.
input: |
  [profile deploy]
  region = ap-southeast-2
  output = text
  aws_access_key_id: {{fixture:aws_key_001}}
sensitive:
  - type: aws_access_key
    fixture: aws_key_001
expected_structure:
  - "[profile deploy]"
  - "region = ap-southeast-2"
  - "output = text"
  - "aws_access_key_id:"
grader:
  kind: json_fields
  expected:
    region: ap-southeast-2
    output: text
    access_key_set: true
placeholder:
  eligible: false
