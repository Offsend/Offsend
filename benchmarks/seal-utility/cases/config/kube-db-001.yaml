id: kube-db-001
category: config
task: |
  Inspect the Kubernetes manifest.
  Return JSON with kind, name, db_host, db_name.
  JSON only. No markdown.
input: |
  apiVersion: v1
  kind: ConfigMap
  metadata:
    name: billing-db
  data:
    DATABASE_URL: postgres://billing:{{fixture:password_006}}@sql.example.com/billing
sensitive:
  - type: password
    fixture: password_006
expected_structure:
  - "kind: ConfigMap"
  - "name: billing-db"
  - "postgres://billing:"
  - "@sql.example.com/billing"
grader:
  kind: json_fields
  expected:
    kind: ConfigMap
    name: billing-db
    db_host: sql.example.com
    db_name: billing
placeholder:
  eligible: true
